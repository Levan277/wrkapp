import 'package:flutter/material.dart';

class Definitions extends StatelessWidget {
  const Definitions({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListView(
      children: [
        Column(
          children: [

          ],
        )
      ],
    );
  }
}


