import  'package:flutter/material.dart';


class SearchUserField extends StatelessWidget {
  const SearchUserField({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
